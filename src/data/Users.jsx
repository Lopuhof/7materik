import React from 'react';

let usersArray = [];

const users = (props) => {
    return (
        <div>
            usersArray.push({
                id: 'loh',
            })
        </div>
    );
};

export default users;